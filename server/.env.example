# Server environment
NODE_ENV=production
PORT=5000

# CORS — comma-separated list of allowed frontend origins
ALLOWED_ORIGINS=https://your-tolz-client.onrender.com

# Optional: structured log level (error | warn | info | debug)
# LOG_LEVEL=info
